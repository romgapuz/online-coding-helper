from app.utils.tool import Tool
import subprocess

class ParseX509Certificate(Tool):
    def execute(self, **kwargs):
        return subprocess.check_output(["openssl", "x509", "-text", "-noout", "-in", kwargs['one']])